<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.basistech</groupId>
    <artifactId>buildtools</artifactId>
    <parent>
        <groupId>com.basistech</groupId>
        <artifactId>textanalytics</artifactId>
        <version>57.2.3</version>
    </parent>
    <version>14.0.0</version>
    <name>Buildtools: Configuration files for Checkstyle and PMD</name>
    <scm>
        <connection>scm:git:git@git.basistech.net:textanalytics/maven-buildtools.git</connection>
        <developerConnection>scm:git:git@git.basistech.net:textanalytics/maven-buildtools.git</developerConnection>
      <tag>buildtools-14.0.0</tag>
  </scm>
    <properties>
        <maven.test.skip>true</maven.test.skip>
        <nexus-staging-plugin-version>1.6.5</nexus-staging-plugin-version>
    </properties>
    <build>
        <defaultGoal>install</defaultGoal>
        <plugins>
            <plugin>
                <!-- This plugin overrides deploy -->
                <groupId>org.sonatype.plugins</groupId>
                <artifactId>nexus-staging-maven-plugin</artifactId>
                <version>${nexus-staging-plugin-version}</version>
                <extensions>true</extensions>
                <configuration>
                    <serverId>basistech.repo</serverId>
                    <nexusUrl>http://nexus.basistech.net:8081/nexus/</nexusUrl>
                    <!-- ID for text-analytics staging profile -->
                    <stagingProfileId>1dd11ca8866422</stagingProfileId>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>pre-site</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <tasks>
                                <mkdir dir="src/main/resources" />
                                <copy file="src/main/resources/checkstyle.xml" todir="src/site/resources" />
                            </tasks>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <!-- cleaning up our copy of checkstyle.xml from the plugin above -->
                            <directory>src/site/resources</directory>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
                <version>2.7</version>
                <reportSets>
                    <reportSet>
                        <reports>
                            <!-- select reports -->
                            <report>help</report>
                            <report>index</report>
                            <report>summary</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>
        </plugins>
    </reporting>
</project>
